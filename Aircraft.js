class Aircraft {
    constructor(msn,registration,type,engine) {
        this.msn=msn;
        this.registration=registration;
        this.type=type;
        this.engine=engine;
    }

    static getById(id){
        const docRef = debugger.collection
    }
}